<%= form_for(current_user.user_follows.build(following_id: @user.id)) do |f| %>
  <div><%= f.hidden_field :following_id %></div>
  <div class="actions">
    <%= f.submit "Follow", class: "btn orange_btn" %>
  </div>
<% end %>
