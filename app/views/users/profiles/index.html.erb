<p id="notice"><%= notice %></p>

<h1 class="users_title"><%= User.model_name.human %></h1>
<table class="main_table">
  <thead>
    <tr>
      <th><%= User.human_attribute_name(:location) %></th>
      <th><%= User.human_attribute_name(:location) %></th>
      <th><%= User.human_attribute_name(:location) %></th>
      <th><%= User.human_attribute_name(:location) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.location %></td>
        <td><%= user.location %></td>
        <td><%= user.location %></td>
        <td><%= user.location %></td>
        <td class="nowrap"><%= link_to t('views.common.show'), user %></td>
        <td class="nowrap"><%= link_to t('views.common.edit'), user_registration_path(user) %></td>
        <td class="nowrap"><%= link_to t('views.common.destroy'), user, method: :delete, data: { confirm: t('views.common.delete_confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="pagination"><%= paginate @users %></div>