<%= form_for(current_user.user_follows.find_by(following_id: @user.id),
html: { method: :delete }) do |f| %>
  <div class="actions">
    <%= f.submit "Unfollow", class: "btn red_btn" %>
  </div>
<% end %>
